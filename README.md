```markdown
# petshop

## Description

A standard software project.

## Installation

1.  Clone the repository: `git clone https://github.com/your-username/petshop.git`
2.  Navigate to the project directory: `cd petshop`
3.  Install dependencies: `npm install` (or `yarn install` if using Yarn)

## Usage

*   **Development:**
    *   Run `npm run dev` to start the development server.
    *   Use the `petshop` command-line tool to interact with the application.
*   **Production:**
    *   Run `npm run production` to deploy the application.
    *   The application will be accessible via the provided URL.
```